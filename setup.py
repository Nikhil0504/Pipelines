from setuptools import setup, find_packages

setup(
    name='Pipelines',
    version='0.1.0',
    author='Nikhil Garuda',
    description='Astronomical data processing pipelines',
    packages=find_packages(),
    python_requires='>=3.9',
)